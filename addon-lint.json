{
    "//": "One rule is not applied. The entry says why, and what would make it apply again. A suppression without that is a lie in a config file.",

    "severity": {
        "testing.addon-testcase": "info"
    },

    "reasons": {
        "testing.addon-testcase": "Statamic\\Testing\\AddonTestCase mocks CP\\Nav::build() to an empty collection and registers exactly one addon provider. This suite needs five providers in a fixed order (Statamic, brand-context, suppression, leadhub, marketing), calls bootAddon() by hand on two of them because Testbench never fires the Statamic::booted callbacks that would, asserts on the nav tree the mock would erase, and flips the storage driver from MARKETING_DRIVER so the whole suite runs twice. tests/TestCase.php documents each with the failure it answers. Revisit when AddonTestCase supports multiple addon providers and stops mocking Nav::build()."
    }
}
